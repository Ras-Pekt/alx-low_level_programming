#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: input variable
 * @b: input variable
 * Return: product of two numbers
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
